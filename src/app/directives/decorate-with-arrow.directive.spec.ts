import { ngxDecorateWithArrow } from './ngx-decorate-with-arrow.directive';

describe('DecorateWithArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new ngxDecorateWithArrow();
    expect(directive).toBeTruthy();
  });
});
